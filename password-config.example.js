// Password configuration - TEMPLATE
// Copy this file to password-config.js and update with your actual password
// The actual password-config.js file is gitignored for security

// This code is obfuscated to make it slightly harder to read, but client-side code is ALWAYS visible
// For truly secure authentication, use server-side validation

(function() {
    // Obfuscated password using simple encoding
    // UPDATE THIS with your actual password characters as an array
    const encoded = ['Y','O','U','R','_','P','A','S','S','W','O','R','D','H','E','R','E'];
    window.SITE_PASSWORD = encoded.join('');
})();

